# Генератор бинарных деревьев

Реализация на Python нерекурсивной генерации бинарных деревьев с настраиваемыми правилами создания узлов.

## Особенности

- **Нерекурсивная реализация** с использованием обхода в ширину на основе очереди
- **Настраиваемые параметры**: высота дерева, значение корня и функции генерации листьев
- **Гибкие значения узлов**: поддерживает любые типы данных
- **Несколько представлений**: деревья на основе словарей и классов
- **Полное тестирование**: полное покрытие тестами с использованием unittest
- **Аннотации типов**: полные подсказки типов для лучшей читаемости кода

## Установка

Внешние зависимости не требуются. Используется только стандартная библиотека Python:

`collections.deque`
`typing`
`unittest`

## Использование

### Базовое использование

```python
from main import gen_bin_tree

# Сгенерировать дерево с параметрами по умолчанию (вариант 12)
tree = gen_bin_tree()
